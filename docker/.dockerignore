# Keep the build context small and reproducible.
# (Build context is the app/ directory — see Dockerfile header.)
node_modules
.next
.git
.env
.env.*
*.log
npm-debug.log*
.DS_Store
# build artifacts / local-only
dist
coverage
*.tsbuildinfo
